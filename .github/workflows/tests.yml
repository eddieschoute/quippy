name: Tests

on: [push,pull_request]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - uses: actions/setup-python@v1
    - uses: dschep/install-pipenv-action@v1
    - name: Run tests
      run: pipenv run python -m test discover test
